[project]
name = "otochef-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "requests==2.34.2",
]

[project.optional-dependencies]
test = [
  "pytest==9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
